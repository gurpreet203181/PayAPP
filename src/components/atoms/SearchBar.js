import React from "react";
import { View,Text, StyleSheet, TouchableOpacity } from "react-native";
import { FONTS,Size,icons } from "../../constants";

const SearchBar = () =>{
    return(
        <View>

        </View>
    )
}

export default SearchBar;